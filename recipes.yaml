recipes:
  xfce-dark-theme:
    name: XFCE Minimal Dark Theme
    description: Applies a minimal dark theme configuration to XFCE desktop environment
    versions:
      v1.0:
        playbook: optional/xfce_dark_theme-v1.0.yaml
        metadata:
          supported_arch:
            - x86_64
            - arm64
          minimum_ram_gb: 1
          options: []